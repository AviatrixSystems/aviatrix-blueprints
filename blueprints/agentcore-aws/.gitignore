# Blueprint-local ignores (repo-root .gitignore covers tfstate / .terraform).

# Generated by terraform-provider-archive at plan time — never commit.
adversary/build.zip

# Podman / Docker build temp (if any).
agent/.dockerignore.swp

# Local dev only
ui/.venv/
ui/__pycache__/
ui/**/__pycache__/
ui/.pytest_cache/
ui/.env
ui/.env.local

# Generated by terraform (see ui-rules.tf)
ui/rules.json
