# Generated by egress-policy/generate.py
# Source: librechat.yaml (sha: 6a779f51481c), .env (sha: fafeb1f78ac5)
# Regenerate: python egress-policy/generate.py --namespace librechat --pod-label app=librechat
# DO NOT EDIT - changes will be overwritten on next git push

apiVersion: networking.aviatrix.com/v1alpha1
kind: FirewallPolicy
metadata:
  name: librechat-egress
  namespace: librechat
spec:
  rules:
    - name: allow-librechat-egress
      selector:
        matchLabels:
          app: librechat
      action: permit
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      webGroups:
        - name: librechat-allowed-domains
      logging: true
    - name: deny-other-egress
      selector:
        matchLabels:
          app: librechat
      action: deny
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      logging: true
  smartGroups:
    - name: anywhere
      selectors:
        - cidr: 0.0.0.0/0
  webGroups:
    - name: librechat-allowed-domains
      domains:
        # --- bedrock sts (IRSA) ---
        - "sts.amazonaws.com"
        # --- bedrock runtime ---
        - "bedrock-runtime.us-east-1.amazonaws.com"
        - "bedrock-runtime.us-west-2.amazonaws.com"
        - "bedrock-runtime.eu-west-1.amazonaws.com"
        # --- custom endpoints ---
        - "api.groq.com"
        - "openrouter.ai"
        # --- remote mcp servers ---
        - "mcp.example.com"
        # --- subprocess mcp package registries ---
        - "pypi.org"
        - "files.pythonhosted.org"
        # --- env-gated ---
        - "api.tavily.com"
        - "github.com"
      # notes:
      # (skipped internal MCP internal-tool: http://host.docker.internal:3001/mcp)
