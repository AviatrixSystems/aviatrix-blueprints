# Generated by utils/egress-policy/generate.py
# Source: librechat.yaml (sha: cf470e8916e3), .env (sha: env-keys)
# Regenerate: python utils/egress-policy/generate.py --namespace librechat --pod-label app=librechat
# DO NOT EDIT - changes will be overwritten on next git push

apiVersion: networking.aviatrix.com/v1alpha1
kind: FirewallPolicy
metadata:
  name: librechat-egress
  namespace: librechat
spec:
  rules:
    - name: allow-librechat-egress
      selector:
        matchLabels:
          app: librechat
      action: permit
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      webGroups:
        - name: librechat-allowed-domains
      logging: true
    - name: deny-other-egress
      selector:
        matchLabels:
          app: librechat
      action: deny
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      logging: true
  smartGroups:
    - name: anywhere
      selectors:
        - cidr: 0.0.0.0/0
  webGroups:
    - name: librechat-allowed-domains
      domains:
        # --- bedrock sts (IRSA) ---
        - "sts.amazonaws.com"
        # --- bedrock runtime ---
        - "bedrock-runtime.us-east-1.amazonaws.com"
        # --- remote mcp servers ---
        - "mcp.example.com"
