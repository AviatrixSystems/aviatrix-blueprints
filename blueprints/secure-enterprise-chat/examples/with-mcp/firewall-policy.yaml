# Generated by utils/egress-policy/generate.py
# Source: librechat.yaml (sha: 1786bc9f4b3f), .env (sha: env-keys)
# Regenerate: python utils/egress-policy/generate.py --namespace librechat --pod-label app.kubernetes.io/name=librechat
# DO NOT EDIT - changes will be overwritten on next git push

apiVersion: networking.aviatrix.com/v1alpha1
kind: FirewallPolicy
metadata:
  name: librechat-egress
  namespace: librechat
spec:
  rules:
    - name: allow-librechat-egress
      selector:
        matchLabels:
          app.kubernetes.io/name: librechat
      action: permit
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      webGroups:
        - name: librechat-allowed-domains
      logging: true
    - name: deny-other-egress
      selector:
        matchLabels:
          app.kubernetes.io/name: librechat
      action: deny
      protocol: tcp
      port: 443
      destinationSmartGroups:
        - name: anywhere
      logging: true
  smartGroups:
    - name: anywhere
      selectors:
        - cidr: 0.0.0.0/0
  webGroups:
    - name: librechat-allowed-domains
      domains:
        # --- always_on: image_registry ---
        - "registry.librechat.ai"
        - "auth.docker.io"
        - "registry-1.docker.io"
        - "production.cloudflare.docker.com"
        # --- always_on: bedrock_sts ---
        - "sts.amazonaws.com"
        # --- bedrock runtime ---
        - "bedrock-runtime.us-east-1.amazonaws.com"
        # --- remote mcp servers ---
        - "mcp.deepwiki.com"
        # --- subprocess mcp package registries ---
        - "registry.npmjs.org"
      # notes:
      # (skipped internal MCP local-tool: http://host.docker.internal:3001/mcp)
